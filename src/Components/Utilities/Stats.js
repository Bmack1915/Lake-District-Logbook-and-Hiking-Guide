const maxWHeight = 978;
const minWHeight = 290;
const minRascent = 30;
const maxRascent = 2134;
const minRdistance = 3.2;
const maxRdistance = 29;
const minRouteTime = Math.ceil(52 / 60);
const maxRouteTime = Math.ceil(624 / 60);

export {
  maxWHeight,
  minWHeight,
  maxRascent,
  minRascent,
  minRdistance,
  maxRdistance,
  minRouteTime,
  maxRouteTime,
};
